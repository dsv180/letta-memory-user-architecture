---
description: "Пример проекта — конфигурация: пути, инструменты, git. version: 1.5"
---
# Конфигурация: Пример проекта

Демонстрационный проект. Показывает, как выглядит `config.md` — техническая
часть проекта: где он лежит на диске, какие инструменты нужны, есть ли git.

## Пути
- **physical_path:** `<путь к папке проекта на диске>` (например, `C:\Projects\example`)
- **MemFS:** `projects/<name_en>/`

## Инструменты
- `<список инструментов и библиотек, которые нужны проекту>`

## Git
- Локальный репозиторий: нет
- Удалённый: нет

## Зависимости
- Нет
