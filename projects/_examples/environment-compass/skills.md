---
description: "Skills relevant to environment-compass project. version: 1.5"
---

# Skills: Environment Compass

Используемые скиллы (загружать через `Skill()` при работе с проектом):

- **github** — публикация релизов, управление репозиторием
- **self-configuration** — установка мода (`letta install .`, `/reload`)
- **context-doctor** — аудит состояния мода

Не используемые в этом проекте:
- xlsx, doc, officecli, playwright, browser-use