---
description: "Memory lifecycle — добавление и удаление файлов, обновление своей версии, выпуск слепка в публичный репозиторий. version: 1.5"
---
# Жизненный цикл памяти
Процедуры: как менять структуру памяти и как выпускать её версии.
Устройство памяти — в [[memory-design/architecture.md]]. Применение архитектуры к своей памяти (внедрение) — в [[memory-design/migration.md]].
Здесь регламент, а не урок: уроки (знание из опыта) живут в `reference/lessons/`, и процедуру выпуска версии туда не дублировать.
Конкретика рабочего репозитория версий (URL, клон, бэкап, скрипты `tools/`) — в локальном справочнике `memory-design/repo.md` (в публичный слепок не входит).

## Добавление файла
1. Определить слой: `system/`, `reference/`, `projects/`, `memory-design/`.
2. `reference/` — дописать ссылку в `lessons.md` или в раздел слоёв architecture.md.
3. `projects/` — запись в `projects/index.json` и `[[path]]`-ссылка в `projects/index.md`.
4. Проставить `version: 1.Х` внутри `description`.

## Удаление файла
1. Убедиться, что информация не дублируется.
2. Убрать ссылку из индекса (`lessons.md` / `index.json` / `index.md`).
3. Удалить файл.

## Обновление версии памяти (1.Х → 1.Ү)
Слепок — снимок структуры памяти; живая память остаётся в `$MEMORY_DIR`.
Новая версия — когда меняется **структура или правила**, а не содержимое проектов.
Версия репозитория — это **тег и Release**, а не папка: файлы версии лежат в корне репозитория.

1. Бэкап всей папки `memory/`.
2. Проверить чистоту живой памяти: `git -C "$env:MEMORY_DIR" status --short` — пусто, иначе разобрать.
3. Привести слои к новой версии, сверить с целевой.
4. Поднять `version` в каждом `description` живой памяти: `tools/bump-version.py <старая> <новая> --root "$env:MEMORY_DIR"`.
5. Скопировать в корень репозитория четыре папки — `system`, `reference`, `projects`, `memory-design` — обязательно с `\*` у источника, иначе папка вложится подпапкой. `system-prompt/` в репозитории уже лежит.
6. Обезличить конвейером `tools/`: он заменит личное и уберёт из слепка локальные файлы (секция `local_files` в общих правилах) — вручную их удалять не нужно.
7. Гейт: `check-anonymized.py` → `exit 0`.
8. Дописать раздел в `CHANGELOG.md`.
9. Коммит и push в ветку репозитория версий.
10. Поставить на новый коммит **новый** тег (`v1.Х`) и опубликовать Release. Теги не переставлять: выпущенный тег неизменяем.

## Обезличивание (обязательный шаг выпуска)
Слепок публикуется в открытый репозиторий, поэтому персональные данные в него не попадают: имена, рабочие пути, реальные проекты, корпоративная специфика.
Обезличивание — воспроизводимый конвейер в самом репозитории (`tools/`), а не ручная правка: ручная слетает при следующей синхронизации.
Правила двухслойные: общий публичный слой + gitignored локальный (имена и организации каждой машины).
Инструменты и команды — в локальном справочнике (в публичный слепок не входит).
**Живая память НЕ обезличивается** — это память агента, а не публикация.
Публикуемый файл не должен ссылаться `[[...]]` на непубликуемый: в слепке такая ссылка ведёт в никуда. Ссылку писать обычным текстом с пометкой «в публичный слепок не входит».

## Смена истории репозитория
`git filter-repo --replace-text` не годится, если в репозитории есть служебные файлы, где сами паттерны хранятся как данные (правила обезличивания): замены применяются ко всей истории и ломают такие файлы.
Способ — **сброс истории**: `git checkout --orphan` → один чистый коммит текущего дерева → force-push в основную ветку + переставить тег.
Обязателен бэкап клона (копия папки целиком) — старая история остаётся только там.
Специфика `filter-repo` на Windows — в [[reference/lessons/git-local.md]].

## Типовые ошибки
- Личное имя искать по корню слова, а не по целой форме: поиск по полной форме не поймает падежи.
- Правило обезличивания проверять на реальной строке: лишний символ в шаблоне (например, `_` в конце пути) делает правило «мёртвым», и оно молча не срабатывает. Единственная страховка — гейт.
- Личные идентификаторы машины (логин ОС, личный e-mail, регион, внутренние хосты, названия организаций) закрываются в локальном слое правил. Чего нет в правилах — гейт не поймает, и утечка уйдёт в публичную историю навсегда.
- Публикуемые файлы-индексы (`projects/PROJECTS.md`, `projects/index.json`) не должны содержать имена реальных проектов — только примеры и плейсхолдеры вида `<проект>`.
- Копирование без `\*` у источника вкладывает папку подпапкой — [[reference/lessons/windows-guide.md]].
- Плейсхолдеры вида `<username>` / `<user>` — не персональные данные; гейт не должен на них срабатывать.
- `git add` с несуществующим путём не стейджит ничего — после коммита проверять `git status --short` и `git log --oneline -1` ([[reference/lessons/git-local.md]]).
- Абсолютный URL на GitHub (`github.com/<логин>/...`) в публикуемом файле: обезличиватель заменит логин на плейсхолдер, и ссылка сломается молча — гейт такое не ловит, если плейсхолдер уже подставлен. В публичных файлах — только относительные ссылки.
